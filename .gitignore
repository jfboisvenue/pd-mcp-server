__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.uv/
uv.lock
.DS_Store
.claude/settings.local.json
# MCPB bundles are produced by CI on tag push, not committed.
*.mcpb
# Default snapshot/restore checkpoints repo (its own git history lives here).
checkpoints/
